Folders:
1. NEMdata: 
	Store raw data downloaded from AEMO database. 
	Then the raw data went through the steps of separating, combining, etc. and the output data were stored in *.mat files 

2. automate the importing and combining process: MATLAB code for preprocessing the data, stored the output data in *.mat
	
3. analysis: MATLAB code for analysis

4. backup matlab code of AUS: backup/checkpoint so don't need to start over again. 
	backup 221005 download raw files: code that downloads raw file from AEMO database, extracting, and save